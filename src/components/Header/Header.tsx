import React from 'react';

const Header: React.FC = () => {
    return (
        <header className='p-4 bg-gray-800'>
            <span className='font-bold text-2xl'>Robin Blog</span>
        </header>
    );
};

export default Header
